<!DOCTYPE html>
<html lang="pt">
    <head>
        <meta charset="utf-8">
        <title>{{ modelo.noModeloCertificado }}</title>
        <style>
            html, body {
                width: 100%;
                height: 100%;
            }

            body {
                background-image: url("data:image/jpeg;base64,{{ imagemCertificado }}");
                background-repeat: no-repeat;
                background-size: 100%;
                background-position: 0 0;
                font-family: sans-serif;
                font-size: 20px;
            }

            .container {
                width: 75%;
                float: left;
                position: absolute;
                top: 46%;
                left: 12.5%;
            }

            .text {
                width: 100%;
                float: left;
                text-align: justify;
                word-wrap: break-word;
            }

            .date {
                width: 100%;
                float: left;
                text-align: right;
                margin-top: 50px;
            }

            .signature {
                width: 100%;
                float: left;
                margin-top: 50px;
                text-align: center;
            }

            .signature span {
                float: left;
            }

            .footer {
                width: 100%;
                float: right;
                position: absolute;
                bottom: 0;
                right: 0;
                text-align: right;
            }
        </style>
    </head>
    <body>
        <div class="container">
            <div class="text">{{ modelo.dsModeloCertificado }}</div>
            <div class="date"><b>&lt;Munícipio/UF, Data de Emissão&gt;</b></div>
            <div class="signature">
                <span>
                    <b>&lt;Nome do Coordenador do Projeto&gt;</b><br>
                    Coordenador do Projeto PET-Saúde
                </span>
            </div>
        </div>
        {% if imagemRodape %}
            <div class="footer">
                <img src="data:image/jpeg;base64,{{ imagemRodape }}" alt="">
            </div>
        {% endif %}
    </body>
</html>

