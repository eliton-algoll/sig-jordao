<!DOCTYPE html>
<html lang="pt">
<head>
    <meta charset="utf-8">
    <title>{{ title }}</title>
    <style>
        html, body {
            width: 100%;
            height: 100%;
        }

        body {
            background-image: url("{{ absolute_url(asset('uploads/' ~ modelo.noImagemCertificado)) }}");
            background-repeat: no-repeat;
            font-family: sans-serif;
            font-size: 20px;
        }

        .container {
            float: left;
            margin: 420px 160px 0;
        }

        .clear {
            clear: both;
        }

        .text {
            float: left;
            text-align: justify;
            word-wrap: break-word;
        }

        .date {
            float: right;
            text-align: right;
            margin-top: 50px;
        }

        .signature {
            float: left;
            margin-top: 50px;
            text-align: center;
        }

        .footer {
            float: right;
            text-align: right;
            position: absolute;
            bottom: 0;
            right: 0;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="text">{{ modelo.dsModeloCertificado | replaceTokenModeloCertificado(projetoPessoa, qtCargaHoraria) }}</div>
        <div class="clear"></div>
        <div class="date">{{ municipio.getNoMunicipio() ~ ' / ' ~ municipio.getSgUf() }}, {{ date|date('d') ~ ' de ' ~ date|date('F')|trans ~ ' de ' ~ date|date('Y') }}</div>
        <div class="clear"></div>
        <div class="signature">
            {{ projetoPessoa.getProjeto().getCoordenadorDeProjeto().getPessoaPerfil().getPessoaFisica().getPessoa().getNoPessoa() }}<br>
            Coordenador do Projeto PET-Saúde
        </div>
    </div>

    {% if modelo.noImagemRodape %}
        <div class="footer">
            <img src="{{ absolute_url(asset('uploads/' ~ modelo.noImagemRodape)) }}" alt="">
        </div>
    {% endif %}
</body>
</html>